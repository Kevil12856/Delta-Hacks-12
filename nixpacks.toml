[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m venv .venv", ". .venv/bin/activate", "pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building...'"]

[start]
cmd = ". .venv/bin/activate && uvicorn agent.server:app --host 0.0.0.0 --port $PORT"
